name: CI

on:
  pull_request:
  # No push trigger, deliberately. This ran on every push to main and the operator asked for it to
  # stop: the gates take roughly half an hour on the Windows lane, and a run nobody is waiting on is
  # a run whose red nobody reads. The cost is real and is accepted rather than hidden -- a direct
  # push to main now runs no gate at all, so coverage, the Windows suite and the AOT IL closure
  # analysis are only as current as the last dispatch or the last pull request.
  #
  # Restoring it is four lines:
  #   push:
  #     branches:
  #       - main
  workflow_dispatch:

permissions:
  contents: read

# One run per branch. Before this, a push that superseded another left both queued against the same
# small pool of macOS runners, and the older run -- testing a commit nobody would act on -- took the
# slot the newer one needed. Deliberately absent from the release workflows: a release must never be
# cancelled by whatever is dispatched next.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  native-sqlcipher-assets:
    # A RID whose hermetic SQLCipher asset is still `pending` cannot be built at all: the targets
    # raise ARCSQLC002 before CopyFilesToOutputDirectory, deliberately, with no probe and no
    # fallback. Reading the manifest here and gating the platform lanes on the result keeps a lane
    # that cannot possibly build from masquerading as a check that merely happens to be red, and
    # the lane comes back on its own the moment the asset is verified and checked in.
    name: Native SQLCipher asset status
    runs-on: ubuntu-latest
    outputs:
      win-x64: ${{ steps.manifest.outputs.win-x64 }}
      win-arm64: ${{ steps.manifest.outputs.win-arm64 }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Read the native asset manifest
        id: manifest
        run: |
          set -euo pipefail
          package=src/RetroDownfall.Arcanum.NativeSqlCipher
          for rid in win-x64 win-arm64; do
            status=$(jq -r --arg rid "$rid" '[.assets[] | select(.rid == $rid)][0].status // "absent"' "$package/native-source-manifest.json")
            path=$(jq -r --arg rid "$rid" '[.assets[] | select(.rid == $rid)][0].path // ""' "$package/native-source-manifest.json")
            if [[ "$status" == "verified" && -n "$path" && -f "$package/$path" ]]; then
              echo "$rid=verified" >> "$GITHUB_OUTPUT"
            else
              echo "$rid=$status" >> "$GITHUB_OUTPUT"
              echo "::warning::No verified hermetic SQLCipher asset for $rid (status: $status). Its CI lane is skipped; build it on a native runner with scripts/build-native-sqlcipher.ps1, verify it with scripts/verify-native-sqlcipher.sh, and check it in."
            fi
          done

  build-test:
    name: Build, test, coverage
    # macOS arm64, not ubuntu: the Grimoire's SQLCipher library is now built by Arcanum and shipped
    # per RID, and Linux is not a shipping RID. A Linux runner has no native asset, so the build
    # fails by design (ARCSQLC002) rather than falling back to an unverified SQLite.
    runs-on: macos-26
    env:
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
      COVERAGE_LINE_TARGET: "80"
      COVERAGE_BRANCH_TARGET: "70"
      # The real-backend credential round trip. It was opt-in and enabled only on the Windows lane,
      # which is gated off until a verified win-x64 SQLCipher asset lands — so no lane exercised a
      # real OS secret store at all, and IOsCredentialStore shipped covered only by its in-memory
      # double. macOS Security.framework is a first-class implementation and this is a disposable
      # runner, so the round trip belongs here too. Verified on macOS: 5 skipped without this, 5
      # passed with it, against the live Keychain.
      ARCANUM_TEST_OS_CREDENTIAL_STORE: true
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # The full suite verifies that the current admission launcher rejects known historical
          # benchmark instruments. Those immutable fixtures are commits, not duplicated files.
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          dotnet-version: "10.0.401"
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/Directory.Packages.props
            .config/dotnet-tools.json

      - name: Restore tools
        run: dotnet tool restore

      - name: Restore and build Arcanum + Compendium + The Forge
        run: |
          set -euo pipefail
          # TheForge.Ux (and through it TheForge.Core) is compiled here because both release
          # workflows package and ship it; without this a compile break merges green.
          for project in \
            src/RetroDownfall.Arcanum.Cli/RetroDownfall.Arcanum.Cli.csproj \
            src/RetroDownfall.TheForge.Ux/RetroDownfall.TheForge.Ux.csproj \
            tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj \
            tests/RetroDownfall.Compendium.Tests/RetroDownfall.Compendium.Tests.csproj \
            tests/RetroDownfall.TheForge.Tests/RetroDownfall.TheForge.Tests.csproj
          do
            dotnet restore "$project"
            dotnet build "$project" -c Release --no-restore
          done

      - name: Test Compendium and The Forge
        run: |
          set -euo pipefail
          # Authoritative Arcanum coverage path is coverage.sh below (do not double-run Arcanum.Tests).
          # The Forge's suite is the sole automated guard for a shipped desktop client — including the
          # markdown image loader's SSRF policy — so a whole-project exclusion here is invisible in
          # every test report. Quarantine individual tests with Skip if it ever needs it again.
          dotnet test tests/RetroDownfall.Compendium.Tests/RetroDownfall.Compendium.Tests.csproj -c Release --no-build
          dotnet test tests/RetroDownfall.TheForge.Tests/RetroDownfall.TheForge.Tests.csproj -c Release --no-build

      # The macOS signing helpers pick an identity out of the operator's keychain, and every way
      # they can pick the wrong one still produces a signature that verifies. Drive them through
      # stubbed `security`/`codesign`; nothing here touches a keychain or signs anything.
      - name: Test the macOS signing helpers
        run: ./scripts/packaging/macos/common_test.sh

      - name: Arcanum tests + coverage thresholds
        run: |
          python3 -m unittest scripts/coverage_threshold_test.py scripts/coverage_workflow_test.py scripts/hosted_producer_analysis_runner_test.py
          ./scripts/coverage.sh --threshold --ci-runtime-only

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: arcanum-coverage-report
          path: |
            .tmp/coverage/report/
            .tmp/coverage/**/coverage.cobertura.xml
            .tmp/coverage/analysis/
          if-no-files-found: warn
          retention-days: 14

  hosted-producer-primary-analysis:
    name: Hosted producer primary analysis
    runs-on: macos-26
    env:
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Setup .NET
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          dotnet-version: "10.0.401"
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/Directory.Packages.props
            .config/dotnet-tools.json

      - name: Build uninstrumented source analysis
        run: dotnet build tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj --configuration Release

      - name: Test source analysis runner
        run: python3 -m unittest scripts/hosted_producer_analysis_runner_test.py

      - name: Test hosted-producer primary analysis
        timeout-minutes: 30
        run: |
          python3 scripts/hosted_producer_analysis_runner.py \
            --project tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj \
            --configuration Release --jobs 2 --timeout-seconds 1740 \
            --partition primary --source-sha "$GITHUB_SHA" \
            --results-directory .tmp/coverage/analysis

      - name: Upload primary source analysis evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: hosted-producer-primary-analysis
          path: .tmp/coverage/analysis/
          retention-days: 14

  hosted-producer-additional-analysis:
    name: Hosted producer additional analysis and fixtures
    runs-on: macos-26
    env:
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Setup .NET
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          dotnet-version: "10.0.401"
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/Directory.Packages.props
            .config/dotnet-tools.json

      - name: Build uninstrumented source analysis
        run: dotnet build tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj --configuration Release

      - name: Test hosted-producer additional analysis and fixtures
        timeout-minutes: 30
        run: |
          python3 scripts/hosted_producer_analysis_runner.py \
            --project tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj \
            --configuration Release --jobs 2 --timeout-seconds 1740 \
            --partition additional --source-sha "$GITHUB_SHA" \
            --results-directory .tmp/coverage/analysis

      - name: Upload additional source analysis evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: hosted-producer-additional-analysis
          path: .tmp/coverage/analysis/
          retention-days: 14

  hosted-producer-analysis:
    name: Hosted producer source analysis
    needs:
      - hosted-producer-primary-analysis
      - hosted-producer-additional-analysis
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Require successful source analysis workers
        run: |
          set -euo pipefail
          if [[ "${{ needs.hosted-producer-primary-analysis.result }}" != "success" \
            || "${{ needs.hosted-producer-additional-analysis.result }}" != "success" ]]; then
            echo "Both hosted-producer source analysis workers must succeed." >&2
            exit 1
          fi

      - name: Download primary source analysis evidence
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: hosted-producer-primary-analysis
          path: .tmp/coverage/analysis-primary

      - name: Download additional source analysis evidence
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: hosted-producer-additional-analysis
          path: .tmp/coverage/analysis-additional

      - name: Reconcile complete source analysis evidence
        run: |
          python3 scripts/hosted_producer_analysis_runner.py \
            --aggregate-primary-results .tmp/coverage/analysis-primary \
            --aggregate-additional-results .tmp/coverage/analysis-additional \
            --source-sha "$GITHUB_SHA" \
            --aggregate-summary .tmp/coverage/analysis-authority/hosted-producer-analysis-summary.json

      - name: Upload source analysis authority
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: hosted-producer-analysis-authority
          path: .tmp/coverage/analysis-authority/
          if-no-files-found: warn
          retention-days: 14

  macos-workspace-check:
    name: macOS workspace-check runtime
    runs-on: macos-26
    env:
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
      ARCANUM_REQUIRE_MACOS_WORKSPACE_CHECK: true
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup .NET
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          dotnet-version: "10.0.401"
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/Directory.Packages.props
            .config/dotnet-tools.json

      - name: Pin trusted root-owned dotnet host
        run: |
          set -euo pipefail
          test -n "${DOTNET_ROOT:-}"
          sudo rm -rf /opt/dotnet
          sudo mkdir -p /opt/dotnet
          sudo cp -R "$DOTNET_ROOT/." /opt/dotnet/
          sudo chown -R root:wheel /opt/dotnet
          sudo chmod -R go-w /opt/dotnet
          /opt/dotnet/dotnet --info

      - name: Build Arcanum tests
        run: |
          dotnet restore tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj
          dotnet build tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj -c Release --no-restore

      - name: Exercise production workspace-check jail
        run: |
          dotnet test tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj \
            -c Release \
            --no-build \
            --no-restore \
            --filter "FullyQualifiedName~Macos_ci_requires_real_workspace_check_production_surface|FullyQualifiedName~MacOsSandbox_DeniesLaunchServicesBrokerEscape|FullyQualifiedName~Real_dotnet_build_uses_seeded_assets|FullyQualifiedName~Real_dotnet_lint_uses_seeded_project_state|FullyQualifiedName~Real_dotnet_test_uses_seeded_assets|FullyQualifiedName~Ci_has_packaged_sqlcipher_native_asset"

      # verify-aot-il-warnings.sh fails closed without ripgrep, which the runner image does
      # not ship. Install it explicitly rather than letting the gate refuse to run.
      - name: Install ripgrep
        run: command -v rg >/dev/null 2>&1 || brew install ripgrep

      - name: Verify macOS AOT IL warnings and search regex smoke
        run: ./scripts/verify-aot-il-warnings.sh

      - name: Build the Grimoire admission benchmark host
        run: >-
          dotnet build tests/RetroDownfall.Arcanum.GrimoireAdmission.Benchmarks/RetroDownfall.Arcanum.GrimoireAdmission.Benchmarks.csproj
          -c Debug --no-incremental -p:RestoreLockedMode=true

      - name: Smoke the Native AOT Grimoire admission benchmark
        run: ./scripts/benchmark-grimoire-admission.sh --smoke

      # The benchmark host is outside RetroDownfall.Arcanum.slnx, so nothing else in this workflow
      # compiles it. The gate below publishes it in Release, but a publish that fails reports as a
      # gate failure rather than as a compile break, and an incremental build hides the analyzer
      # diagnostics a clean one raises. Naming the project here is also what puts it inside the
      # compiled-project closure ContinuousIntegrationWorkflowTests walks.
      - name: Build the Covenant benchmark host
        run: |
          dotnet build tests/RetroDownfall.Arcanum.Covenant.Benchmarks/RetroDownfall.Arcanum.Covenant.Benchmarks.csproj \
            -c Debug \
            --no-incremental

      # The gate runs here too: its absolute ceilings carry enough headroom for a shared runner, and a
      # breach on this lane is the signal it exists to give. The run is recorded as well as gated, so
      # the lane leaves behind the observed distribution its headroom is justified against; without an
      # artifact the margin between what a shared runner measures and what the ceilings allow is a
      # claim nobody can check.
      - name: Covenant benchmark gate
        run: ./scripts/benchmark-covenant.sh --gate --record covenant-benchmark-run.json

      # Retention-pinned and unconditional: the gate step above exits 1 on a ceiling breach, and the
      # breaching run is the one whose distribution is most worth reading. The host writes the JSON
      # before it evaluates the gate, so the file is there either way. `if: always()` is a step-level
      # key, which the job parser ContinuousIntegrationWorkflowTests uses ignores -- it filters to
      # job-level indentation before it reads a key -- so this does not mark the job conditional and
      # does not exempt it from the native-SQLCipher runner check, exactly as the same condition on
      # the coverage upload in build-test already does not.
      - name: Upload the Covenant benchmark run
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: covenant-benchmark-run
          path: covenant-benchmark-run.json
          if-no-files-found: warn
          retention-days: 90

  windows-suite:
    # Focused native/OS contracts run on each shipping Windows architecture. Portable runtime
    # coverage is authoritative on macOS; ARM64 also checks actual Windows compiler inputs.
    #
    # The bare name is left alone on purpose. A repository's required-status-check configuration
    # refers to a workflow lane by this exact string, external to this file, so renaming it would
    # silently break whatever branch protection names it.
    name: Windows test suite
    # The hermetic SQLCipher targets refuse to build a RID whose asset is not verified (ARCSQLC002)
    # long before the first test executes. Gate on the manifest so a lane in that state skips
    # honestly instead of failing at `dotnet build`, and returns by itself once the asset lands.
    # The gate stays now that win-x64's asset is checked in: nothing in the job would run without
    # it, so the condition is the honest statement of the prerequisite rather than a leftover.
    needs: native-sqlcipher-assets
    if: needs.native-sqlcipher-assets.outputs.win-x64 == 'verified'
    runs-on: windows-latest
    env:
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
      # Turns a lane that silently skipped everything into a red build. See WindowsCiSurfaceTests.
      ARCANUM_REQUIRE_WINDOWS_SUITE: true
      # The credential round-trip is the one test that writes to a real OS secret store, so it is
      # opt-in and stays off on developer machines. A disposable runner is where it belongs.
      ARCANUM_TEST_OS_CREDENTIAL_STORE: true
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup .NET
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          dotnet-version: "10.0.401"
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/Directory.Packages.props
            .config/dotnet-tools.json

      - name: Restore and build Arcanum + Compendium
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # The Forge's suite runs once, on build-test; this lane exists for the Windows-gated
          # Arcanum surface, so re-running a platform-agnostic suite here would buy nothing.
          foreach ($project in @(
            'tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj',
            'tests/RetroDownfall.Compendium.Tests/RetroDownfall.Compendium.Tests.csproj'
          )) {
            dotnet restore $project
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
            dotnet build $project -c Release --no-restore
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }

      - name: Test Arcanum
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # Keep the native/OS surface explicit; portable runtime coverage runs on macOS.
          dotnet test tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj `
            -c Release --no-build --no-restore --filter "Category!=Perf&Category!=HostedProducerAnalysis&(FullyQualifiedName~RetroDownfall.Arcanum.Tests.Platform.WindowsCiSurfaceTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.NativeSqlCipher.NativeSqlCipherDeliveryTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.NativeSqlCipher.SqlCipherCompatibilityTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Security.OsCredentialStoreRoundTripTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Platform.ProcessResourceLimiterWindowsBehaviorTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Platform.WindowsJobObjectSessionTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Process.WindowsAppContainerPolicyTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Process.WindowsAppContainerRestoreJournalTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Process.ChildProcessBoundaryBehaviorTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Process.ChildProcessFilesystemJailTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Familiars.FamiliarExecutableResolverTests|FullyQualifiedName=RetroDownfall.Arcanum.Tests.GrimoireTransitions.GrimoireOfflineTransitionJournalFileStoreTests.Windows_delete_durably_succeeds_after_publication|FullyQualifiedName=RetroDownfall.Arcanum.Tests.GrimoireTransitions.GrimoireOfflineTransitionJournalFileStoreTests.Windows_read_fails_closed_when_the_published_file_acl_is_weakened|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Cli.FullInstallationResetAttestationFileReaderTests.Reader_rejects_a_file_not_controlled_only_by_the_current_owner|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Mcp.WorkspacePathPolicySymlinkTests.IsPathUnderWorkspace_OnWindows_IgnoresDirectoryNameCase|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Platform.ProcessResourceLimiterTests.Apply_returns_assign_after_start_on_windows|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Security.HttpsCertificateLoaderTests.Load_PemPair_OnWindows_ZeroesTheExportedPkcs12Buffer|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Security.SecureFileReaderTests.TryOpenRegularFile_on_windows_rejects_reparse_symlink|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Workspaces.PhysicalFileSystemBrowserTests.ListAsync_And_ReadAsync_NormalizeBackslashesInRelativePaths_OnWindows|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Diagnostics.MaintenanceLockCheckTests.Windows_read_only_stale_lock_is_degraded_and_its_bytes_are_unchanged|FullyQualifiedName=RetroDownfall.Arcanum.Tests.GrimoireTransitions.GrimoireOfflineTransitionJournalFileStoreTests.Windows_compare_unlink_removes_the_name_while_the_retained_evidence_handle_stays_readable|FullyQualifiedName=RetroDownfall.Arcanum.Tests.GrimoireTransitions.GrimoireOfflineTransitionJournalFileStoreTests.Windows_real_primitives_reinspect_then_publish_a_new_working_file|FullyQualifiedName=RetroDownfall.Arcanum.Tests.GrimoireTransitions.GrimoireOfflineTransitionJournalFileStoreTests.Windows_no_replace_rename_preserves_both_files_when_destination_exists|FullyQualifiedName=RetroDownfall.Arcanum.Tests.GrimoireTransitions.GrimoireOfflineTransitionJournalFileStoreTests.Windows_exchange_through_the_real_primitives_retains_authentic_predecessor_identity|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Storage.AtomicFileTests.ReplaceAsync_rejects_existing_file_with_multiple_hard_links|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Mcp.FileHandleIdentityTests.TryGetPathMetadata_hard_link_reports_multiple_links|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Configuration.ConfigurationPresetPersistenceTests.Journal_cleanup_reports_a_denied_delete_instead_of_throwing)"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Test Compendium
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          dotnet test tests/RetroDownfall.Compendium.Tests/RetroDownfall.Compendium.Tests.csproj `
            -c Release --no-build --no-restore --filter "FullyQualifiedName=RetroDownfall.Compendium.Ux.Tests.Compendium.ConfigurationStoreSmokeTests.WriteAsync_hardens_the_destination_that_arrived_with_loose_permissions"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # The AOT diagnostic gate fails closed when ripgrep is absent. Windows runner images do not
      # promise it, so install the pinned fallback before asking the gate to classify diagnostics.
      - name: Install ripgrep
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          if (-not (Get-Command rg -CommandType Application -ErrorAction SilentlyContinue)) {
            choco install ripgrep --version=15.2.0 --yes --no-progress
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }

          rg --version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # The shipping publish below is warning-free because it suppresses dependency summary
      # diagnostics after this gate has classified the detailed warnings. Run the diagnostic profile
      # on this native architecture first, or a win-x64-only first-party warning is invisible.
      - name: Audit win-x64 Native AOT diagnostics
        shell: bash
        run: |
          set -euo pipefail
          rg --version
          ./scripts/verify-aot-il-warnings.sh win-x64

      - name: Verify warning-free win-x64 shipping executable and provider-contract smoke
        shell: bash
        run: ./scripts/verify-shipping-publish.sh --rid win-x64

  windows-arm64-suite:
    # The release ships win-arm64 alongside win-x64 and osx-arm64: it is built, packaged, signed and
    # handed to operators. Until this lane, not one test had ever executed on it. Three things it
    # reaches that windows-suite cannot, none of which x64 coverage can stand in for:
    #
    #   - A different binary. runtimes/win-arm64/native/e_sqlcipher.dll is its own hermetic build
    #     from the pinned sources, so the Grimoire's every P/Invoke into SQLCipher crosses the arm64
    #     calling convention here for the first time. A marshalling mistake that x64 tolerates --
    #     a struct layout, a blittable assumption, a pointer width taken for granted -- surfaces on
    #     this lane and nowhere else in CI.
    #   - Windows on ARM, not Windows. Credential Manager, Job Objects, AppContainer and the NTFS
    #     ACL surface are reached through the same P/Invoke declarations, on an OS build that runs
    #     x64 code under emulation. That emulation is the local failure mode: it lets a tool answer
    #     confidently about the wrong architecture, which is why build-windows.yml stopped believing
    #     PROCESSOR_ARCHITECTURE and why the SDK assertion below exists.
    #   - The RID's own delivery. This is the only lane that runs the suite with the win-arm64 asset
    #     actually loaded, so the hermetic targets' promise of exactly one library and no fallback is
    #     observed for this RID here rather than inferred from the manifest saying `verified`.
    name: Windows test suite (arm64)
    # Gated on its own RID, not on x64's. The asset statuses are independent -- either can go
    # pending on its own -- and a lane gated on the other architecture's asset would fail at
    # `dotnet build` with ARCSQLC002 rather than skip honestly, which is the property the manifest
    # gate exists to preserve.
    needs: native-sqlcipher-assets
    if: needs.native-sqlcipher-assets.outputs.win-arm64 == 'verified'
    # A whole job rather than a matrix leg of windows-suite, deliberately, for two reasons that both
    # fail silently. `needs:` names a job id and waits for every leg of that job's matrix, so folding
    # arm64 into windows-suite would put this lane behind anything that gates on that job id, without
    # anyone choosing that -- and this lane has never run, so its real failure list is unknown. And a
    # job-level `if:` cannot read the `matrix` context at all, so a matrix could not carry the
    # per-RID manifest gate above without rebuilding it as a dynamically generated matrix. Please do
    # not "simplify" the duplication away without answering both.
    #
    # Each Windows architecture tests on its own architecture for the reason build-windows.yml
    # publishes on its own: `windows-11-arm` is free for this repository because it is public.
    runs-on: windows-11-arm
    env:
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
      # Both variables carry over from the x64 lane unchanged, and for unchanged reasons. Without
      # the first, a lane whose Windows-gated tests all skipped reports a clean green while
      # asserting nothing -- see WindowsCiSurfaceTests, which is architecture-agnostic and holds
      # here exactly as it does there. The second opts in to the one test that writes to a real OS
      # secret store; this runner's Credential Manager is as disposable as the x64 runner's, and
      # arm64 is where the store's P/Invoke surface is least exercised, so opting out here would
      # skip the round trip on the architecture that most needs it.
      ARCANUM_REQUIRE_WINDOWS_SUITE: true
      ARCANUM_TEST_OS_CREDENTIAL_STORE: true
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup .NET
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          dotnet-version: "10.0.401"
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/Directory.Packages.props
            .config/dotnet-tools.json

      # The SDK's own host RID, because Windows on ARM runs an x64 SDK under emulation without
      # complaining. That degradation is silent in the one direction that matters here: an emulated
      # x64 run resolves the win-x64 native asset, passes every test, and reports arm64 coverage this
      # lane never obtained -- a second copy of windows-suite wearing an arm64 label. Assert it
      # before anything is built rather than inferring it from the runner label, which can be right
      # while the toolchain is not. build-windows.yml makes the same assertion before it publishes.
      - name: Assert the SDK is native to win-arm64
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"

          $info = (& dotnet --info) -join "`n"

          Write-Host $info

          $match = [regex]::Match($info, '(?m)^\s*RID:\s*(?<rid>\S+)\s*$')

          if (-not $match.Success) {
              throw "dotnet --info did not report a host RID, so this lane cannot tell whether it is testing win-arm64 or an emulated win-x64."
          }

          if ($match.Groups['rid'].Value -ne 'win-arm64') {
              throw "This lane must run a win-arm64 SDK; this runner's host RID is $($match.Groups['rid'].Value), so the suite would exercise win-x64 a second time under emulation."
          }

      - name: Restore and build Arcanum + Compendium
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # The same two projects the x64 lane builds, for the same reason: The Forge's suite is
          # platform-agnostic and runs once, on build-test.
          foreach ($project in @(
            'tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj',
            'tests/RetroDownfall.Compendium.Tests/RetroDownfall.Compendium.Tests.csproj'
          )) {
            dotnet restore $project
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
            dotnet build $project -c Release --no-restore
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }

      - name: Test Arcanum
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # Same explicit native/OS surface as x64, exercised with the arm64 asset and SDK.
          dotnet test tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj `
            -c Release --no-build --no-restore --filter "Category!=Perf&Category!=HostedProducerAnalysis&(FullyQualifiedName~RetroDownfall.Arcanum.Tests.Platform.WindowsCiSurfaceTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.NativeSqlCipher.NativeSqlCipherDeliveryTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.NativeSqlCipher.SqlCipherCompatibilityTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Security.OsCredentialStoreRoundTripTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Platform.ProcessResourceLimiterWindowsBehaviorTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Platform.WindowsJobObjectSessionTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Process.WindowsAppContainerPolicyTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Process.WindowsAppContainerRestoreJournalTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Process.ChildProcessBoundaryBehaviorTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Process.ChildProcessFilesystemJailTests|FullyQualifiedName~RetroDownfall.Arcanum.Tests.Familiars.FamiliarExecutableResolverTests|FullyQualifiedName=RetroDownfall.Arcanum.Tests.GrimoireTransitions.GrimoireOfflineTransitionJournalFileStoreTests.Windows_delete_durably_succeeds_after_publication|FullyQualifiedName=RetroDownfall.Arcanum.Tests.GrimoireTransitions.GrimoireOfflineTransitionJournalFileStoreTests.Windows_read_fails_closed_when_the_published_file_acl_is_weakened|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Cli.FullInstallationResetAttestationFileReaderTests.Reader_rejects_a_file_not_controlled_only_by_the_current_owner|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Mcp.WorkspacePathPolicySymlinkTests.IsPathUnderWorkspace_OnWindows_IgnoresDirectoryNameCase|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Platform.ProcessResourceLimiterTests.Apply_returns_assign_after_start_on_windows|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Security.HttpsCertificateLoaderTests.Load_PemPair_OnWindows_ZeroesTheExportedPkcs12Buffer|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Security.SecureFileReaderTests.TryOpenRegularFile_on_windows_rejects_reparse_symlink|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Workspaces.PhysicalFileSystemBrowserTests.ListAsync_And_ReadAsync_NormalizeBackslashesInRelativePaths_OnWindows|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Diagnostics.MaintenanceLockCheckTests.Windows_read_only_stale_lock_is_degraded_and_its_bytes_are_unchanged|FullyQualifiedName=RetroDownfall.Arcanum.Tests.GrimoireTransitions.GrimoireOfflineTransitionJournalFileStoreTests.Windows_compare_unlink_removes_the_name_while_the_retained_evidence_handle_stays_readable|FullyQualifiedName=RetroDownfall.Arcanum.Tests.GrimoireTransitions.GrimoireOfflineTransitionJournalFileStoreTests.Windows_real_primitives_reinspect_then_publish_a_new_working_file|FullyQualifiedName=RetroDownfall.Arcanum.Tests.GrimoireTransitions.GrimoireOfflineTransitionJournalFileStoreTests.Windows_no_replace_rename_preserves_both_files_when_destination_exists|FullyQualifiedName=RetroDownfall.Arcanum.Tests.GrimoireTransitions.GrimoireOfflineTransitionJournalFileStoreTests.Windows_exchange_through_the_real_primitives_retains_authentic_predecessor_identity|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Storage.AtomicFileTests.ReplaceAsync_rejects_existing_file_with_multiple_hard_links|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Mcp.FileHandleIdentityTests.TryGetPathMetadata_hard_link_reports_multiple_links|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Configuration.ConfigurationPresetPersistenceTests.Journal_cleanup_reports_a_denied_delete_instead_of_throwing)"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # These checks evaluate actual Windows compiler inputs without traversing producer roots.
      - name: Verify Windows compiler inputs
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          dotnet test tests/RetroDownfall.Arcanum.Tests/RetroDownfall.Arcanum.Tests.csproj `
            -c Release --no-build --no-restore --filter "Category!=Perf&(FullyQualifiedName=RetroDownfall.Arcanum.Tests.Operations.HostedGrimoireProducerInventoryTests.ProductionCompilationsResolveGeneratedJsonSymbols|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Operations.HostedGrimoireProducerInventoryTests.ProductionCompilerGraphUsesCanonicalProjectReferences|FullyQualifiedName=RetroDownfall.Arcanum.Tests.Operations.HostedGrimoireProducerInventoryTests.ProductionIncludesEveryFirstPartyDependencyAndApiCliRoots)"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Test Compendium
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          dotnet test tests/RetroDownfall.Compendium.Tests/RetroDownfall.Compendium.Tests.csproj `
            -c Release --no-build --no-restore --filter "FullyQualifiedName=RetroDownfall.Compendium.Ux.Tests.Compendium.ConfigurationStoreSmokeTests.WriteAsync_hardens_the_destination_that_arrived_with_loose_permissions"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # The AOT diagnostic gate fails closed when ripgrep is absent. The arm64 runner can execute
      # the pinned fallback under emulation; product tests and publication remain native arm64.
      - name: Install ripgrep
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          if (-not (Get-Command rg -CommandType Application -ErrorAction SilentlyContinue)) {
            choco install ripgrep --version=15.2.0 --yes --no-progress
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }

          rg --version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # This must execute on the arm64 SDK asserted above. The shipping publish deliberately uses
      # its warning-free profile, so only this diagnostic profile can reject an arm64-only
      # first-party IL/AOT warning before the product is accepted.
      - name: Audit win-arm64 Native AOT diagnostics
        shell: bash
        run: |
          set -euo pipefail
          rg --version
          ./scripts/verify-aot-il-warnings.sh win-arm64

      - name: Verify warning-free win-arm64 shipping executable and provider-contract smoke
        shell: bash
        run: ./scripts/verify-shipping-publish.sh --rid win-arm64

  aot-il:
    name: Native AOT IL and executable gates
    # Both executable smokes open the Grimoire, so this host RID needs its verified native
    # SQLCipher asset just like build-test.
    runs-on: macos-26
    env:
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup .NET
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          dotnet-version: "10.0.401"
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/Directory.Packages.props
            .config/dotnet-tools.json

      # verify-aot-il-warnings.sh fails closed without ripgrep, which the runner image does
      # not ship. Install it explicitly rather than letting the gate refuse to run.
      - name: Install ripgrep
        run: command -v rg >/dev/null 2>&1 || brew install ripgrep

      # The gate itself has silent failure modes (a warning misfiled as third-party, an ILC
      # assertion satisfied by another project's log). Test the gate before trusting it; this
      # stubs `dotnet` and compiles nothing.
      - name: Test the AOT IL gate
        run: ./scripts/verify_aot_il_warnings_test.sh

      - name: Verify AOT IL warnings and regex smoke (host RID)
        run: ./scripts/verify-aot-il-warnings.sh

      - name: Verify warning-free shipping executable and provider-contract smoke
        run: ./scripts/verify-shipping-publish.sh --rid osx-arm64

  static-analysis:
    # These tools ship in the repository, so their own regression tests and checks must run here.
    # This job compiles nothing and runs in under a minute, which is what makes it affordable to gate on.
    name: Formatting and shell lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Test the C# blank-line formatter
        run: python3 -m unittest discover -s scripts/tests -p 'test_*.py'

      - name: Verify C# blank-line formatting
        run: python3 scripts/align_csharp_blanklines.py --repo . --check

      # The runner image ships shellcheck today, but a gate that silently vanishes when the
      # image changes is worse than no gate, so install it when it is missing.
      - name: Install shellcheck
        run: command -v shellcheck >/dev/null 2>&1 || (sudo apt-get update && sudo apt-get install -y shellcheck)

      # -P SCRIPTDIR resolves `source "$SCRIPT_DIR/common.sh"` the way bash will at runtime;
      # find rather than a glob so a script added in a new packaging directory cannot escape.
      - name: Validate shell scripts
        run: |
          set -euo pipefail
          find scripts -name '*.sh' -print0 | xargs -0 shellcheck -x -P SCRIPTDIR

      # Two things nothing else here checks. The `run:` blocks inside a workflow are shell that the
      # find above never sees, because they are not files. And a reusable-workflow call is checked by
      # nothing at all until it is dispatched: a `with:` key the callee does not declare, or an
      # expression referencing a context that is not available where it sits, first shows up as a
      # failed run on the day somebody reaches for a release. actionlint reads both, and hands the
      # embedded scripts to the shellcheck installed above.
      #
      # Installed through the Go module proxy rather than from a release page, so the download is
      # verified against the checksum database instead of trusted for having the right URL.
      - name: Install actionlint
        run: go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.12

      - name: Validate workflows
        run: |
          set -euo pipefail
          "$(go env GOPATH)/bin/actionlint"
