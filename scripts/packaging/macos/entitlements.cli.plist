<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    The macOS CLI is a self-contained JIT CoreCLR publish (Cli.csproj disables PublishAot for
    osx-* RIDs), so the hardened runtime must be told to permit the RWX/MAP_JIT mappings the
    runtime creates. Without allow-jit the signed binary aborts before Main with
    "Failed to create CoreCLR, HRESULT: 0x80070008".
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
